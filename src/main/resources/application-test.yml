spring:
  datasource:
    url: jdbc:h2:mem:oms-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000

app:
  seed:
    enabled: false
  jwt:
    secret: test-only-jwt-secret-0123456789abcdef0123456789
    ttl-minutes: 60
